version: "3.8"

services:
  chat-server:
    build: .
    ports:
      - "12345:12345"
    environment:
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=12345
      - MAX_CONNECTIONS=50
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
